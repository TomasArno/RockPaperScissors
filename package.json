{
  "name": "RockPaperScissors",
  "version": "1.0.0",
  "repository": "https://github.com/TomasArno/RockPaperScissors",
  "author": "Tomás Arno <arnotomas1@gmail.com>",
  "license": "MIT",
  "scripts": {
    "dev": "parcel serve ./src/index.html",
    "build": "parcel build ./src/index.html --dist-dir build --public-url /RockPaperScissors",
    "deploy": "gh-pages -d build",
    "build-and-deploy": "yarn build && yarn deploy"
  },
  "devDependencies": {
    "@types/node": "^18.14.0",
    "parcel": "^2.8.3"
  },
  "dependencies": {
    "gh-pages": "^5.0.0"
  }
}
